{
  "name": "nginx-cf",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --input-type=module -e \"import('./src/index.js').then(() => console.log('syntax ok'))\""
  },
  "devDependencies": {
    "wrangler": "^3.57.2"
  }
}
