{
  "name": "emby-cf",
  "version": "2.0.0",
  "description": "Emby 反代加速 · Cloudflare Worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --input-type=module -e \"import('./src/index.js').then(() => console.log('syntax ok'))\""
  },
  "devDependencies": {
    "wrangler": "^3.57.2"
  }
}
